{
  "private": true,
  "scripts": {
    "build": "gatsby build",
    "build:pp": "gatsby build --prefix-paths",
    "dev": "gatsby develop",
    "serve": "gatsby serve"
  },
  "dependencies": {
    "gatsby": "^2.0.91",
    "smooth-doc": "^0.0.2"
  }
}
